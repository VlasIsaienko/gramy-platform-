-- ============================================================
-- Миграция: запретить повторную регистрацию игрока в одной
-- категории турнира
-- ============================================================
-- Как применить: Supabase → твой проект → SQL Editor →
-- вставить этот файл целиком → Run.
--
-- Таблица registrations сейчас пустая, поэтому constraint
-- применяется сразу (без NOT VALID).
-- ============================================================

alter table registrations
  add constraint registrations_category_player_unique unique (category_id, player_id);
