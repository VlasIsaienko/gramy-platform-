-- ============================================================
-- Миграция: поддержка форматов Mexicano / Americano
-- ============================================================
-- Как применить: Supabase → твой проект → SQL Editor →
-- вставить этот файл целиком → Run.
--
-- Число раундов задаётся администратором один раз перед генерацией
-- 1-го раунда и дальше не пересматривается. Корты переиспользуют
-- существующее matches.group_number (номер корта внутри раунда) —
-- новая колонка для этого не нужна.
-- ============================================================

alter table categories add column total_rounds integer;
